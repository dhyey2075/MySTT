# Renderer static assets

Place **`logo.png`** here so `/logo.png` resolves (shell logo + favicon).  

Recommended: copy the same file as `apps/desktop/resources/icons/logo.png` used for the Electron window/tray/build icons.
